name: rust
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ASrunner
    steps:
      - uses: actions/checkout@v2
      - run: |
          cd rust/src
          rustc hello.rs
          ./hello
      - run: |
          rustup install nightly
          rustup show
      - run: |
          rust/scripts/cargo.ps1
        shell: pwsh